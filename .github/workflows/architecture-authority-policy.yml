name: Architecture Authority Policy

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  authority-policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify Veragensia architecture authority constitution
        run: bash tests/03-veragensia-architecture-authority-policy-static-test.sh
