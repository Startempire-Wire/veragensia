# foot terminal for the Veragensia public demo.
# The 1280-wide desktop gets scaled down hard on phones, so the terminal font
# runs larger than foot's default for readability on small viewports.
font=monospace:size=13
# Subtle Work-view-consistent colors.
[colors]
background=17131f
foreground=e6e0f8
